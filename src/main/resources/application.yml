spring:
  datasource:
    url: jdbc:mysql://${URL}:${PORT}/todolistdb
    username: ${USERNAME}
    password: ${PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update